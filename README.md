A simple templating engine that takes an input template and json file and returns an HTML file.

Clone the repo

From the terminal command line, enter 'chmod +x templater'

ln -s $PWD/templater /usr/local/bin/
